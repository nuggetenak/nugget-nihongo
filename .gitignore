# ── Not deployed (build/dev tools only) ─────────────
agents/
tools/
logs/
state/
# docs/ is intentionally tracked — project knowledge base
# _MAP.md and session recaps are tracked — project governance

# ── Environment ──────────────────────────────────────
*.env
.env.*
config/local*.json
config/*.local.json

# ── Dependencies ─────────────────────────────────────
node_modules/
npm-debug.log*
yarn-error.log

# ── OS files ─────────────────────────────────────────
.DS_Store
Thumbbs.db
*.swp
*~

# ── Temporary ────────────────────────────────────────
*.tmp
*.bak
/tmp/
pk-nugget-nihongo/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example
